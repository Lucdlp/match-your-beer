All beers

<% params[:query].split(', ').each do |param| %>
  <%= param %>
<% end %>


<% @beers.each do |beer| %>
  <%= link_to beer.name, beer_path(beer) %>
  <%= beer.brewery %>
  <%= beer.country %>
  <%= beer.alcohol_level %>
<% end %>
